# Python dependencies for the EU Football Analytics Airflow image.
# Installed on top of the official apache/airflow:2.10.5-python3.11 base image.

# Google Cloud provider for Airflow (GCS and BigQuery hooks used in the DAG)
apache-airflow-providers-google==10.25.0

# dbt Core + BigQuery adapter (called via BashOperator in the DAG)
dbt-core==1.9.4
dbt-bigquery==1.9.1

# Kaggle API client (used in download_dataset.py)
kaggle==2.0.0